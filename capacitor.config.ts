import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RenTierAngularV2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
